#/usr/bin/env bash
# Just a collection of auto completion commands for my bash custom scripts.

complete -W "mono multi" chdis
complete -W "fr uk down" proton
complete -W "up down" rpivpn
complete -W "-clean -month -archive -location" notes
